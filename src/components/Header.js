import React from 'react';
import NavBar from './NavBar'
var Header = React.createClass({
  render: function() {
    return (
      <div className>
        <NavBar />
      </div>
    )
  }

})

export default Header;
