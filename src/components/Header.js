import React from 'react';
import NavBar from './NavBar'
var Header = React.createClass({
  render: function() {
    return (
      <div>
        <NavBar />
      </div>
    )
  }

})

export default Header;
